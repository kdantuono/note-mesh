# NoteMesh Backend

FastAPI backend for the NoteMesh note sharing platform.

## Setup

```bash
pip install -e .
pip install -e .[dev]
```

## Development

```bash
uvicorn notemesh.main:app --reload
```